<% unless @user.is_followed_by?(current_user) %>
  <%= form_for Relationship.new do |f| %>
    <%= f.hidden_field :followed_id, value: @user.id %>
    <%= f.submit "Follow", class: "btn btn-default" %>
  <% end %>
<% end %>